class Solution
{
    public:
    //Function to delete a node without any reference to head pointer.
    void deleteNode(Node *del)
    {
       // Your code here
       if(!del->next)
       return;
       Node *node=del->next;
       del->data=node->data;
       del->next=node->next;
       free(node);
    }

};
